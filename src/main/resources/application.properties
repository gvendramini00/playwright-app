# Application name
spring.application.name=MyPlayRightApp

# Database connection (replace values as needed)
spring.datasource.url=jdbc:mysql://192.168.16.56:3306/OGP?useSSL=false&serverTimezone=UTC
spring.datasource.username=tecnalis
spring.datasource.password=tecnalis2002

# JDBC driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate/JPA settings
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Prevent schema generation (important let false since we don't want to change the schema)
spring.jpa.generate-ddl=false


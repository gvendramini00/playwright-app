# ── Dev profile ──────────────────────────────────────────────────────────────
# Activate with: --spring.profiles.active=dev
# Or set env var: SPRING_PROFILES_ACTIVE=dev
#
# This profile provides dev credential defaults so the app starts locally
# without needing environment variables set. Override any value by setting
# the corresponding env var (e.g. CGM_DB_USERNAME overrides the default below).

# Show SQL in dev for easier debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Watch the browser locally — flip to true to run headless
playwright.headless=false

# ── Alira Back Office (dev defaults) ─────────────────────────────────────────
alira.username=${ALIRA_USERNAME:gvendramini}
alira.password=${ALIRA_PASSWORD:56689272vG!}
alira.login-url=${ALIRA_LOGIN_URL:https://gms.dev.tecnalis.com/alira-server/login.jsp}
alira.base-url=${ALIRA_BASE_URL:https://gms.dev.tecnalis.com/alira-server/}

# ── CGM datasource (dev defaults) ────────────────────────────────────────────
datasource.cgm.url=${CGM_DB_URL:jdbc:mysql://192.168.16.56:3306/OGP?useSSL=false&serverTimezone=UTC}
datasource.cgm.username=${CGM_DB_USERNAME:tecnalis}
datasource.cgm.password=${CGM_DB_PASSWORD:tecnalis2002}

# ── GP-PT datasource (dev defaults) ──────────────────────────────────────────
datasource.gppt.url=${GPPT_DB_URL:jdbc:mysql://192.168.16.21:3306/OGP?useSSL=false&serverTimezone=UTC}
datasource.gppt.username=${GPPT_DB_USERNAME:tecnalis}
datasource.gppt.password=${GPPT_DB_PASSWORD:tecnalis2002}
